//Math é um objeto do JS
//Tem uma função que calcula a potencia, porque o JS não tem um operador para isso.

//Exemplo de aplicação

const raio = 5.6
const area = Math.PI * Math.pow(raio, 2) //Math.pow(raio, 2) mesma coisa que raio elevado ao quadrado.

console.log(area)
console.log(typeof Math)